apiVersion: v1
kind: Service
metadata:
  name: ops-metrics
  namespace: agent-office
  labels: {app.kubernetes.io/name: ops-metrics, app.kubernetes.io/part-of: supply-chain-services}
spec:
  selector: {app.kubernetes.io/name: ops-metrics}
  ports: [{name: http, port: 8080, targetPort: 8080}]
